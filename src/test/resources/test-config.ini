# Test port forwarding configuration

[aws]
region = eu-west-1
profile = test-profile
remote_port = 8080

[services]
# format: name = local_port, skip [, remote_port]
service-alpha = 7001, false
service-beta = 7002, true
service-gamma = 7003, false, 9090 # custom remote port
